<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.UnipiCityVibe" parent="Theme.Design.Light.NoActionBar">

        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:textAllCaps">false</item>

        <item name="textColor">@color/black</item>
        <item name="textColorReverse">@color/white</item>
        <item name="textErrorColor">@color/dark_red</item>
        <item name="textSuccessColor">@color/dark_green</item>
        <item name="primaryColor">@color/very_dark_gray</item>
        <item name="backgroundColor">@color/very_light_gray</item>
    </style>

    <style name="Theme.UnipiCityVibe" parent="Base.Theme.UnipiCityVibe" />

</resources>